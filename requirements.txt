# T-25 (21.08.2026): раньше зависимости нигде не были зафиксированы —
# на новой машине/VPS приходилось угадывать, что ставить через pip, по
# ошибкам ImportError. Список собран по факту import в *.py на 21.08.2026.
gspread
google-auth
flask
requests
ddgs
gunicorn
telethon
python-socks
# T-77 (25.08.2026): удаление водяного знака с фото winner_auto_club —
# headless-сборка opencv, т.к. на VPS нет дисплея/GUI-либ (opencv-python
# тянет их и не ставится на голом сервере).
opencv-python-headless
numpy
# T-85 (26.08.2026): детект бейджа winner_auto_club переделан с template
# matching на OCR (см. userbot_parser.py, remove_watermark). pytesseract —
# это только Python-обвязка, САМ БИНАРЬ tesseract-ocr нужно ставить system
# package manager'ом ОТДЕЛЬНО (pip его не поставит):
#   apt install tesseract-ocr
# Без этого шага на VPS remove_watermark() будет тихо пропускать все фото
# (фото уходят как есть, без ошибки — см. TesseractNotFoundError в коде).
pytesseract
# T-86 (27.08.2026): авто-монтаж коротких роликов из видео winner_auto_club
# (auto_montage.py) зовёт системный бинарь ffmpeg через subprocess (склейка,
# вертикальная раскладка, сведение звука) — ставится ТОЖЕ system package
# manager'ом, pip его не поставит:
#   apt install ffmpeg
# Без этого шага build_short() упадёт с FileNotFoundError при первом же
# вызове ffmpeg — в отличие от remove_watermark()/tesseract, здесь нет
# отдельного safety-net на случай отсутствия бинаря, ошибка попадёт в лог
# ("ошибка при авто-монтаже видео — шлю оригинал", видео уйдёт необработанным).
